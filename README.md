# Chatbot

Simple user interface for OpenAI API

Azure staging instance at https://chatbot-staging.happyfield-34b6208e.northeurope.azurecontainerapps.io

# Development

1. Copy `.env.template` as `.env` file and add you OpenAI API key

2. Run `npm i` and `npm start` to setup and start the development environment
